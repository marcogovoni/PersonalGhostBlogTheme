<div class="checkout-plan card h-100">
  <header class="checkout-plan__hdr m-b flex flex-col flex-cc text-acc-1 p-b border-bottom-1">
    <h3 class="checkout-plan__title m-b-xs">{{t "Yearly"}}</h3>
    <div class="checkout-plan__price">
      <sup>{{price currency=@price.currency}}</sup><strong>{{@price.yearly}}</strong>
    </div>
    <a class="btn btn--brand btn--sm btn--rounded" 
      href="javascript:void(0)" data-members-plan="Yearly">
      {{#if @member}}{{t "Upgrade"}}{{else}}{{t "Subscribe now"}}{{/if}}
    </a>
  </header>
  <div class="checkout-plan__content text-acc-3">
    <ul class="list--unstyled m-b-0">
      <li>{{t "Full access to all private posts"}}</li>
      <li>{{t "Regular updates with new content"}}</li>
      <li>{{t "Support independent publishing"}}</li>
      <li>{{t "Simple, secure card payment"}}</li>
      <li>{{t "Regular newsletters"}}</li>
      <li>{{t "No advertisement"}}</li>
      <li>{{t "One easy payment instead of 12!"}}</li>
    </ul>
  </div>
</div>